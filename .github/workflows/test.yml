name: Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  backend-tests:
    name: Backend Tests
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run pytest with coverage
      working-directory: ./backend
      run: python -m pytest -v --cov=. --cov-report=term --cov-report=xml

  # Frontend tests would go here when implemented
  # frontend-tests:
  #   name: Frontend Tests
  #   runs-on: ubuntu-latest
  #   steps:
  #   - uses: actions/checkout@v4
  #   - name: Setup Node.js
  #     uses: actions/setup-node@v4
  #     with:
  #       node-version: '18'
  #   - name: Install dependencies
  #     working-directory: ./frontend
  #     run: npm ci
  #   - name: Run tests
  #     working-directory: ./frontend
  #     run: npm test
