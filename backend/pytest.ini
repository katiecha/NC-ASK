[pytest]
# Simple pytest configuration for NC-ASK backend tests

# Add backend to Python path so imports work
pythonpath = .

# Handle async tests automatically
asyncio_mode = auto

# Show verbose output
addopts = -v
